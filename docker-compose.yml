services:
  backend1:
    build: .
    environment:
      - PORT=8080
      - ID=backend1
      - LATENCIA_MS=0
    ports:
      - "8081:8080"

  backend2:
    build: .
    environment:
      - PORT=8080
      - ID=backend2
      - LATENCIA_MS=200
    ports:
      - "8082:8080"

  backend3:
    build: .
    environment:
      - PORT=8080
      - ID=backend3
      - LATENCIA_MS=500
    ports:
      - "8083:8080"